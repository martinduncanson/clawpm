---
complexity: s
created: '2026-07-03'
id: CLAWP-072-001
parent: CLAWP-072
predictions:
  confidence: 5
  duration_min: 20
  success_criteria:
  - Exactly one @main.group inbox registration remains in cli.py
  - clawpm inbox send/read/ack/thread behave identically (existing inbox tests pass
    unmodified)
priority: 5
scope:
- src/clawpm/cli.py
updated: '2026-07-10'
---

# Delete duplicate inbox command group in cli.py

Two byte-identical inbox blocks exist: cli.py:5649-5751 and cli.py:5766-5868 (import block + group + send/read/ack/thread). Click registers the LATER @main.group("inbox") so the earlier block is dead code (~120 lines). Independently confirmed by three audit agents 2026-07-03.

SPEC: delete one block (they are identical, so either; keep the later one to minimise diff churn near line numbers used elsewhere, or the earlier - verify identity first with a diff of the two ranges). No behaviour change expected. Run the inbox test files to prove it.

PITFALL: verify byte-identity before deleting - if the blocks have drifted, reconcile to the live (later) one.

## Notes

