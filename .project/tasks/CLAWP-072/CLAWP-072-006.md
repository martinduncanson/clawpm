---
complexity: s
created: '2026-07-03'
id: CLAWP-072-006
parent: CLAWP-072
predictions:
  confidence: 4
  duration_min: 45
  success_criteria:
  - tasks add --parent persists predictions, scope and success_criteria identically
    to parentless add
  - Regression test covers the subtask path
priority: 5
scope:
- src/clawpm/tasks.py
- src/clawpm/cli.py
---

# Fix silent prediction/scope drop on subtask add (--parent path)

Found 2026-07-03 while filing this very batch: clawpm tasks add --parent N silently discards --predict-*, --confidence, --predicted-by, --success-criteria and --scope (frontmatter ends up with only complexity/priority/parent), while the parentless path persists them. Repair path: tasks edit works. Issue logged in .agent/issues.jsonl (2026-07-03T17:41Z).

SPEC: thread the prediction/scope kwargs through the subtask-creation path (suspect add_subtask in tasks.py); a flag the CLI accepts must either persist or error - never silently drop (fail-open-not-fail-silent principle, CLAWP-039/041 lineage). Add a regression test asserting subtask add persists predictions identically to parentless add. Note tasks edit also cannot set filled_by - decide whether --predicted-by belongs on edit too.

## Notes

