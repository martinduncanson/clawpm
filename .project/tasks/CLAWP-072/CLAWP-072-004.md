---
complexity: s
created: '2026-07-03'
id: CLAWP-072-004
parent: CLAWP-072
predictions:
  confidence: 4
  duration_min: 30
  success_criteria:
  - clawpm tasks emit-rubric --rubric-format outcome-payload works; no collision with
    global --format
  - Existing emit-rubric tests updated and passing
priority: 5
scope:
- src/clawpm/cli.py
---

# Rename emit-rubric --format to --rubric-format (global flag collision)

Global --format/-f takes json|text (cli.py:156) but tasks emit-rubric declares a LOCAL --format taking markdown|outcome-payload (cli.py:2361). Same flag name, different value sets by position: clawpm tasks emit-rubric --format json errors with invalid choice. Audit 2026-07-03.

SPEC: rename the local flag to --rubric-format. DECISION: whether to keep --format as a deprecated alias for one release - given no external consumers beyond our own skills/playbooks, prefer a clean rename; grep skills/, docs/, README for emit-rubric usages and update them in the same commit.

## Notes

