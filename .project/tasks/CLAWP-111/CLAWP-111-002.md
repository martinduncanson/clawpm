---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-111-002
leaf_key: decision-maps::root-sections-fog
out_of_scope:
- auto-populating Decisions so far (L1 owns it)
- emit-tree root fields (L3)
parallel_group: 1
parent: CLAWP-111
predictions:
  approach: Two new Task fields (omit-when-default); a small section renderer used
    by show; --graduates as an option on tasks add that runs after the child is persisted,
    in the same file lock. UNGROUNDED-GRAPH effort.
  complexity: m
  confidence: 4
  duration_min: 100
  filled_by: agent
  pre_mortem: Prefix matching on free text is ambiguous; the error path listing candidates
    is the safety net and must be tested.
  success_criteria:
  - comparator: eq:pass
    criterion: 'WHEN a task has destination or not_yet_specified in frontmatter THEN
      tasks show renders ## Destination, ## Decisions so far, ## Not yet specified,
      ## Out of scope sections in that order; absent fields render no section'
    gradeable_signal: render tests for present/absent combinations
  - comparator: eq:pass
    criterion: WHEN tasks add --parent <root> --graduates "<text>" matches exactly
      one fog entry (exact or case-insensitive prefix) THEN the child is created and
      that entry is removed from the root; zero or multiple matches error listing
      candidates and create nothing
    gradeable_signal: CLI tests for one/zero/many matches
  - comparator: eq:pass
    criterion: WHEN tasks fog <root> --add/--drop run THEN the list changes and nothing
      else in the file changes
    gradeable_signal: diff test on the task file
priority: 5
scope:
- src/clawpm/models.py
- src/clawpm/tasks.py
- src/clawpm/cli/tasks.py
- src/clawpm/output.py
- tests/
stop_conditions:
- If the body renderer cannot be extended without rewriting existing task bodies,
  STOP and propose storing sections only in frontmatter.
updated: '2026-09-02'
---
# A root task is a map: destination + not_yet_specified sections, fog graduates via tasks add --graduates

## Notes

