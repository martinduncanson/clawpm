---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-111-001
leaf_key: decision-maps::kind-decision-resolution
out_of_scope:
- emit-tree changes (L3)
- frontier query (L4)
- planner docs (L5)
parallel_group: 1
parent: CLAWP-111
predictions:
  approach: Add kind Literal + resolution/resolved_at to Task; extend parsing/serialisation
    with omit-when-default; guard in the done transition (services layer, not CLI-only);
    append to parent body via the existing frontmatter/body writer; tag reflection
    event. UNGROUNDED-GRAPH effort.
  complexity: m
  confidence: 4
  duration_min: 120
  filled_by: agent
  pre_mortem: The done path is shared by shortcuts.done and tasks state; guarding
    only one leaves a bypass.
  success_criteria:
  - comparator: eq:pass
    criterion: 'WHEN a task file has kind: decision THEN Task.kind loads as decision
      and a task without the key loads as build with no frontmatter diff on save'
    gradeable_signal: 'tests: load/save round-trip for both kinds; existing fixture
      files unchanged byte-for-byte'
  - comparator: eq:pass
    criterion: WHEN clawpm done <id> runs on a kind:decision task without --resolution
      THEN it errors decision_needs_resolution and the task stays open
    gradeable_signal: CLI test asserting error code and unchanged state
  - comparator: eq:pass
    criterion: 'WHEN done --resolution runs on a decision leaf with a parent THEN
      frontmatter resolution/resolved_at are set and the parent body gains one line
      under ## Decisions so far formatted - [<title>](<id>): <first line>'
    gradeable_signal: test reads parent file after done and asserts the section and
      line
  - comparator: eq:pass
    criterion: 'WHEN a decision task closes THEN its task_done reflection event carries
      kind: decision'
    gradeable_signal: test reads the JSONL event
priority: 5
scope:
- src/clawpm/models.py
- src/clawpm/tasks.py
- src/clawpm/services/tasks.py
- src/clawpm/cli/tasks.py
- src/clawpm/cli/shortcuts.py
- src/clawpm/reflect.py
- tests/
stop_conditions:
- If adding kind requires changing the directory-encoded state model, STOP and report;
  kind is a frontmatter attribute, not a state.
- If tasks edit cannot preserve the new fields without fixing CLAWP-108 first, STOP
  and report the dependency rather than fencing it locally.
updated: '2026-09-02'
---
# A task can be a decision: kind:decision, done requires --resolution, parent map gains a Decisions-so-far line

## Notes

