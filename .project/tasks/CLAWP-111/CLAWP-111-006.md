---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-111-006
leaf_key: decision-maps::skill-working-rules-plugin-commands
out_of_scope:
- new CLI logic
- the harness-side ~/.claude/skills/clawpm mirror (sync hook handles tracked content)
parallel_group: 3
parent: CLAWP-111
predictions:
  approach: 'Docs + three command files + manifest edit; verify with claude plugin
    validate and a local marketplace reinstall (note: plugin CLI writes live settings.json
    - re-diff before trusting the PR).'
  complexity: s
  confidence: 4
  duration_min: 60
  filled_by: agent
  pre_mortem: SKILL.md mirror at ~/.claude/skills/clawpm is a checkout whose SKILL.md
    was untracked once; confirm the file is tracked so the sync hook carries it.
  success_criteria:
  - comparator: eq:present
    criterion: 'skills/clawpm/SKILL.md has a Working the map section: next --frontier
      -> start (claim) -> resolve -> done --resolution -> graduate fog; one decision
      leaf per session with agent research leaves exempt; names wrap ids in narration;
      human-delegable leaves are never answered by the agent'
    gradeable_signal: section present and consistent with L1/L2/L4 CLI surfaces
  - comparator: eq:pass
    criterion: commands/clawpm-plan.md, clawpm-chart.md, clawpm-next.md exist, each
      <=15 lines of routing into the clawpm-planner skill or clawpm next, and .claude-plugin/marketplace.json
      lists them
    gradeable_signal: claude plugin validate passes locally; commands appear in the
      plugin listing after reinstall
priority: 5
scope:
- skills/clawpm/SKILL.md
- commands/
- .claude-plugin/marketplace.json
stop_conditions:
- If marketplace.json commands schema differs from the memory reference (claude plugin
  marketplace schema), STOP and check the live schema before guessing.
updated: '2026-09-02'
---
# Working a map is documented and reachable: SKILL.md rules (one decision per session, refer by name, HITL never self-answers) and plugin commands /clawpm:plan /clawpm:chart /clawpm:next

## Notes

