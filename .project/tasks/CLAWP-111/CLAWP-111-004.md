---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-111-004
leaf_key: decision-maps::frontier-query
out_of_scope:
- changing get_next_task's non-frontier behaviour
- cross-project frontiers
parallel_group: 2
parent: CLAWP-111
predictions:
  approach: Reuse list_tasks parent/child linking + depends satisfaction from get_next_task
    + active-lease set from leases.py; one pure function frontier(config, project,
    root) shared by both commands. UNGROUNDED-GRAPH effort.
  complexity: s
  confidence: 4
  duration_min: 90
  filled_by: agent
  pre_mortem: 'Lease staleness: an expired lease must not count as claimed; use the
    existing lease-expiry rule, don''t reinvent it.'
  success_criteria:
  - comparator: eq:pass
    criterion: WHEN next --frontier <root> runs THEN JSON has frontier (open children
      at any depth whose depends are all done and that hold no active lease), blocked
      [{id, waiting_on}], claimed [{id, lease_holder}], ordered by priority then id
    gradeable_signal: fixture tree with one of each case; assert membership and order
  - comparator: eq:pass
    criterion: WHEN text format is used THEN every task is printed as its title with
      the id in brackets after it, never a bare id
    gradeable_signal: text output test
  - comparator: eq:pass
    criterion: WHEN the root has no open children THEN the command reports the map
      is clear with exit 0
    gradeable_signal: empty case test
priority: 5
scope:
- src/clawpm/tasks.py
- src/clawpm/cli/shortcuts.py
- src/clawpm/cli/tasks.py
- src/clawpm/leases.py
- tests/
stop_conditions:
- If lease lookup requires a per-task file read that makes the query O(n^2) on large
  trees, STOP and report before optimising.
updated: '2026-09-02'
---
# An agent can see the frontier: clawpm next --frontier <root> and tasks list --frontier <root>

## Notes

