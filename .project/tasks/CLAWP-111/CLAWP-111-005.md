---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-111-005
leaf_key: decision-maps::planner-chart-mode-docs
out_of_scope:
- core code
- importing mattpocock or osmani skill files verbatim
parallel_group: 3
parent: CLAWP-111
predictions:
  approach: Docs-only PR against the planner skill; source text is ADR D5 plus the
    wayfinder/osmani/feature-dev mechanics cited there. Keep SKILL.md under its current
    length budget by moving detail into references.
  complexity: m
  confidence: 4
  duration_min: 90
  filled_by: agent
  pre_mortem: Chart mode written as an option gets ignored; write it as a rule with
    a trigger.
  success_criteria:
  - comparator: eq:present
    criterion: SKILL.md documents chart mode with its trigger (>=2 open decisions
      block decompose, or operator asks), its output (decision map via emit-tree with
      kind:decision leaves, depends_refs, destination, fog), its stop rule, and the
      no-fog exit
    gradeable_signal: section present; a reviewer can execute chart mode from the
      text alone against examples/decision-map.emit.json
  - comparator: eq:present
    criterion: stage-playbook.md recon names feature-dev:code-explorer as the fan-out
      agent when installed with the return-key-files-then-read rule; specify carries
      the interview discipline (one question at a time, facts looked up not asked,
      false-yes rejected); l/xl ideate compares minimal/clean/pragmatic lenses and
      recommends one
    gradeable_signal: three sections present and cross-linked from SKILL.md
  - comparator: eq:present
    criterion: decompose-vet.md recommends WHEN/THEN criterion phrasing and defines
      the default stop-list (auth/session, schema migration, bulk deletion, secrets,
      payment, prod config) seeded into stop_conditions unless waived in the PRD;
      scale-dial.md places chart mode between m and l
    gradeable_signal: sections present; example leaves in examples/ updated to the
      phrasing
  - comparator: eq:pass
    criterion: All example documents under skills/clawpm-planner/examples pass emit-tree
      --dry-run after the edits
    gradeable_signal: existing example-validation test green
priority: 5
scope:
- skills/clawpm-planner/SKILL.md
- skills/clawpm-planner/references/
- skills/clawpm-planner/examples/
stop_conditions:
- 'If L3 has not merged, STOP: the decision-map example cannot validate without depends_refs/kind
  support.'
updated: '2026-09-02'
---
# clawpm-planner can chart: chart mode, interview discipline, explorer/architect fan-out, WHEN/THEN criteria, default stop-list

## Notes

