---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-111-003
leaf_key: decision-maps::emit-tree-depends-refs-kind
out_of_scope:
- planner prose (L5)
- Task model fields (L1/L2 must land first)
parallel_group: 2
parent: CLAWP-111
predictions:
  approach: Extend ALLOWED_LEAF_KEYS/ALLOWED_TOP_KEYS; resolve refs after ids are
    minted, before promote; topological check in validate. UNGROUNDED-GRAPH effort.
  complexity: m
  confidence: 3
  duration_min: 120
  filled_by: agent
  pre_mortem: Ref resolution happens after id minting but promote is all-or-nothing;
    a resolution failure after staging must roll back the staged dir.
  success_criteria:
  - comparator: eq:pass
    criterion: 'WHEN a leaf carries kind: decision and depends_refs: [refs] THEN the
      minted tasks have kind set and depends resolved to the minted ids of those refs,
      atomically with the rest of the tree'
    gradeable_signal: emit test asserting depends on the persisted files
  - comparator: eq:pass
    criterion: WHEN depends_refs names an unknown ref or forms a cycle THEN --dry-run
      and the real emit both fail validation with no writes
    gradeable_signal: tests for unknown ref and 2-node cycle
  - comparator: eq:pass
    criterion: WHEN depends_refs contains id:<existing-task-id> under attach_to THEN
      the dependency resolves to that id
    gradeable_signal: attach_to test
  - comparator: eq:pass
    criterion: skills/clawpm-planner/examples/decision-map.emit.json exists and passes
      clawpm tasks emit-tree --dry-run; references/emission-contract.md documents
      the four new keys
    gradeable_signal: CI example-validation test includes the new example
priority: 5
scope:
- src/clawpm/emit_tree.py
- skills/clawpm-planner/references/emission-contract.md
- skills/clawpm-planner/examples/
- tests/
stop_conditions:
- If L1 or L2 have not merged, STOP; this leaf depends on Task.kind, destination and
  not_yet_specified existing.
updated: '2026-09-02'
---
# emit-tree can emit a decision map: leaf kind + depends_refs, root destination + not_yet_specified, validated example

## Notes

