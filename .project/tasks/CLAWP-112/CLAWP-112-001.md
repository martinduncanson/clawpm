---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-112-001
leaf_key: calibration::two-line-ledger-prediction-id
out_of_scope:
- scoring (M4)
- rewriting historical JSONL
parallel_group: 1
parent: CLAWP-112
predictions:
  approach: Mint id in Predictions post-init when absent (persisted on first save);
    one helper write_prediction_event(event, ...) in reflect.py used by all three
    write sites; done/blocked events read prediction_id from the task. UNGROUNDED-GRAPH
    effort.
  complexity: m
  confidence: 3
  duration_min: 150
  filled_by: agent
  pre_mortem: Three write sites means one gets missed (likely emit-tree); the per-site
    tests are the guard.
  success_criteria:
  - comparator: eq:pass
    criterion: WHEN predictions are first written by tasks add, tasks predict, or
      emit-tree THEN predictions.prediction_id (uuid4 hex) is stored in frontmatter
      and a prediction_registered event with the snapshot, registered_at, filled_by,
      baseline_ref is appended to the task's reflection JSONL
    gradeable_signal: tests per write site asserting frontmatter and JSONL line
  - comparator: eq:pass
    criterion: WHEN tasks edit changes any predictions field THEN a prediction_revised
      event with the full new snapshot is appended and unrelated prediction fields
      are preserved (CLAWP-108 fixed or covered by a failing-then-passing test)
    gradeable_signal: edit round-trip test
  - comparator: eq:pass
    criterion: 'WHEN task_done or task_blocked events are written THEN they carry
      prediction_id; legacy events without it are read as legacy: true registered
      at created'
    gradeable_signal: reader tests on mixed fixtures
  - comparator: eq:0.6
    criterion: WHEN a synthetic corpus has 10 registered and 6 resolved non-voided
      predictions THEN closure reads 0.6 with open_predictions 4
    gradeable_signal: unit test on the closure function (consumed by M4)
priority: 5
scope:
- src/clawpm/models.py
- src/clawpm/reflect.py
- src/clawpm/tasks.py
- src/clawpm/services/tasks.py
- src/clawpm/cli/tasks.py
- src/clawpm/emit_tree.py
- tests/
stop_conditions:
- If the concurrency lock (append_jsonl_line) cannot be used from the tasks add path
  without a circular import, STOP and report; do not write unlocked.
updated: '2026-09-02'
---
# Predictions are pre-registered: prediction_id + prediction_registered/prediction_revised events, closure computable

## Notes

