---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-112-002
leaf_key: calibration::honest-actuals
out_of_scope:
- changing the work_log schema
- re-deriving actuals for historical events
parallel_group: 1
parent: CLAWP-112
predictions:
  approach: Extend _compute_actuals with the gap sum; add Actuals.active_min; remove
    task_complexity passthrough; new --actual-complexity option threaded to the transition.
    UNGROUNDED-GRAPH effort.
  complexity: s
  confidence: 4
  duration_min: 90
  filled_by: agent
  pre_mortem: Removing the complexity passthrough breaks tests that asserted the tautology;
    update them, don't keep the tautology.
  success_criteria:
  - comparator: eq:pass
    criterion: WHEN a task has work_log entries THEN actuals.active_min = sum of min(gap_to_next,
      60) over consecutive entries plus 15 for the last, and active_min <= duration_min
      on every real corpus event
    gradeable_signal: unit test with a hand-built log; property check over ~/clawpm/reflections
      replayed from a fixture copy
  - comparator: eq:pass
    criterion: WHEN done runs without --actual-complexity THEN actuals.complexity
      is null and deltas.complexity_match is null; WHEN given THEN complexity_match
      compares prediction to the supplied value
    gradeable_signal: CLI tests both branches; test_reflect_phase1 updated to the
      new contract
  - comparator: eq:pass
    criterion: reflect summarize and find_reference_tasks read active_min when present
      and fall back to duration_min, reporting which was used
    gradeable_signal: summarize output field duration_source in {active,wallclock}
priority: 5
scope:
- src/clawpm/reflect.py
- src/clawpm/models.py
- src/clawpm/cli/tasks.py
- src/clawpm/cli/shortcuts.py
- tests/
stop_conditions:
- 'If the 60-minute gap cap has to be configurable to make tests meaningful, STOP:
  it is a constant in the spec; propose a spec change instead.'
updated: '2026-09-02'
---
# Actuals are honest: active_min from work_log gaps; actuals.complexity only from --actual-complexity

## Notes

