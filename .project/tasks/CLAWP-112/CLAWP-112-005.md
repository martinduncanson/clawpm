---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-112-005
leaf_key: calibration::nudges-closure-alarm
out_of_scope:
- weekly cadence integration
- auto-applying anything
parallel_group: 4
parent: CLAWP-112
predictions:
  approach: Reuse M4 functions; hook path already fail-open. UNGROUNDED-GRAPH effort.
  complexity: s
  confidence: 4
  duration_min: 75
  filled_by: agent
  pre_mortem: Hook latency; measure before wiring.
  success_criteria:
  - comparator: eq:pass
    criterion: WHEN the current project has a metareflection_due entry THEN clawpm
      hook eval-stop output and clawpm done text output include one line naming the
      category and count; WHEN none THEN no line
    gradeable_signal: hook and CLI output tests
  - comparator: eq:pass
    criterion: WHEN rolling-30-day closure < 0.6 for the portfolio THEN reflect summarize
      and scorecard include an alarm string; the threshold is a named constant cited
      to spec 2.3
    gradeable_signal: boundary test at 0.59/0.60
  - comparator: eq:pass
    criterion: Nudges never change exit codes or block a transition
    gradeable_signal: exit-code assertions in the same tests
priority: 5
scope:
- src/clawpm/cli/hook.py
- src/clawpm/cli/shortcuts.py
- src/clawpm/cli/tasks.py
- src/clawpm/reflect.py
- tests/
stop_conditions:
- If computing metareflection_due inside the Stop hook exceeds ~200ms on the real
  portfolio, STOP and cache it in the summarize output file instead of recomputing
  per stop.
updated: '2026-09-02'
---
# Calibration signals reach the agent: Stop-hook and done output surface metareflection_due and a closure alarm below 0.6

## Notes

