---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-112-003
leaf_key: calibration::miss-category
out_of_scope:
- backfill
- repeat-rule computation (M4)
- replacing surprise_taxonomy
parallel_group: 2
parent: CLAWP-112
predictions:
  approach: MISS_CATEGORIES frozenset next to SURPRISE_TAXONOMY; resolve_axes(predictions,
    actuals, deltas) -> per-axis + held per spec 2.1, living in reflect.py so M4 reuses
    it; nudge via click.echo to stderr in text mode, a warnings field in JSON mode.
    UNGROUNDED-GRAPH effort.
  complexity: s
  confidence: 4
  duration_min: 90
  filled_by: agent
  pre_mortem: The nudge becomes a blocking prompt; agent closes must stay non-interactive.
  success_criteria:
  - comparator: eq:pass
    criterion: WHEN done --miss-category <c> runs THEN task_done carries miss_category=c
      for c in {wrong_signal, wrong_weight, missed_variable, operator_override, noise,
      structural}; any other value is rejected with bad_miss_category and nothing
      is written
    gradeable_signal: CLI tests valid/invalid
  - comparator: eq:pass
    criterion: WHEN the derived held outcome is incorrect and no --miss-category was
      given THEN done prints a one-line nudge listing the six values and still closes
      the task; WHEN held is correct or unresolvable THEN no nudge
    gradeable_signal: output tests for the three branches
  - comparator: eq:pass
    criterion: surprise_taxonomy behaviour is unchanged and both fields may be set
      on the same event
    gradeable_signal: existing test_reflection_phase1_5 green plus a combined test
priority: 5
scope:
- src/clawpm/models.py
- src/clawpm/reflect.py
- src/clawpm/cli/tasks.py
- src/clawpm/cli/shortcuts.py
- tests/
stop_conditions:
- If computing held at done-time requires the full metrics module from M4, STOP and
  implement only the per-axis resolution function from spec 2.1 here (M4 imports it).
updated: '2026-09-02'
---
# Misses get a root cause: --miss-category on done, validated six-value taxonomy, nudge on unclassified miss

## Notes

