---
baseline_ref: f776124
created: '2026-09-02'
delegability: agent
id: CLAWP-112-004
leaf_key: calibration::metrics-summarize-scorecard
out_of_scope:
- hook nudges (M5)
- gbrain writes
- 30-day learning velocity (spec excludes it)
parallel_group: 3
parent: CLAWP-112
predictions:
  approach: New module clawpm/calibration.py (pure functions over event lists) called
    from summarize_calibration; scorecard command in cli/reflect.py; synthetic corpus
    builder in tests with documented expected numbers. UNGROUNDED-GRAPH effort.
  complexity: m
  confidence: 3
  duration_min: 180
  filled_by: agent
  pre_mortem: Learning velocity gets implemented on 30-day windows because it is natural;
    the spec forbids it at current volume.
  success_criteria:
  - comparator: eq:pass
    criterion: 'WHEN reflect summarize runs THEN JSON contains calibration with brier,
      calibration_curve, overconfidence_ratio, mae_log, bias{factor,direction}, bucket_bias_factor,
      learning_velocity{90d,365d}, miss_category_distribution, metareflection_due,
      closure{rate,open_predictions,void_rate}; every cell has n and either values
      or insufficient_data: true when n<20'
    gradeable_signal: schema test on a synthetic corpus; run on the real portfolio
      exits 0
  - comparator: eq:pass
    criterion: WHEN the synthetic corpus has hand-computed expected values (documented
      in the test) THEN brier, mae_log, bias.factor, overconfidence_ratio and closure
      match to 4 decimal places using the confidence->p map from spec 2.2
    gradeable_signal: golden-value unit tests
  - comparator: eq:pass
    criterion: WHEN the same miss_category appears >=3 times in the trailing 30 days
      within one project THEN metareflection_due lists {project_id, miss_category,
      count}; 2 occurrences or a 31-day spread do not
    gradeable_signal: boundary tests
  - comparator: eq:5
    criterion: 'WHEN reflect scorecard runs THEN it prints exactly five lines: closure,
      bias factor, Brier+n, overconfidence headline, top miss category / metareflection
      due; --json returns the calibration block'
    gradeable_signal: output test
  - comparator: eq:pass
    criterion: kind:decision events are excluded from the duration axis by default
      and included with --include-decisions
    gradeable_signal: flag test
priority: 5
scope:
- src/clawpm/reflect.py
- src/clawpm/cli/reflect.py
- tests/
stop_conditions:
- 'If M1, M2 or M3 have not merged, STOP: this leaf consumes prediction_id/closure,
  active_min and resolve_axes.'
- If any formula needs a definition not in spec 2.3, STOP and propose the spec change
  rather than inventing it in code.
updated: '2026-09-02'
---
# reflect summarize reports the calibration block (spec 2.3) and reflect scorecard renders it in five lines

## Notes

