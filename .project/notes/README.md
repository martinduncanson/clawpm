# Operator notes

Operator-facing notes for the clawpm project. Referenced by the project CLAUDE.md ("read these before starting work").

Convention: one file per note, named `YYYY-MM-DD_<slug>.md`. Created 2026-07-03 during the full project audit (directory previously referenced but missing).
